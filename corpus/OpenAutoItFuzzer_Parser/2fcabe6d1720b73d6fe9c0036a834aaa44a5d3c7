[0l If 31Tse Else$ ==========================lseäE¦ls 1Else Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfunc2Else Else =If 3Thenä- 2ThenäEæseIf 2Else If 1Then¬ElseIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfuncIf 2Else Else$ If 2ThenäElseIf nFunc g()
Fu======lseäEBinaryls 2Else Else$ If 2ThenäElseIf nFunc g()
f nfunc2Else Else =If 3ShenäElse Else IflseIf nääElseäElse=If3ThenäElse Else If 1ThenäElseIf 1Ef nFFuncäElseIf seIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäE”Œš¼f nsElseIf 2Else Else$ If 2ThenäElseIf nFunc g()
Funcc2Ele ElsElseIf 2Else ElsElse Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfunc2Else$Else Ifÿ3ThenäElse$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfunc3Else Else IThenäEl# g()
FuncäE”Œš¼f nsElseIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf seIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäE”Œš¼f nsElseIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfuncncäElseIf seIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäE”Œš¼f nsElseIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfunc2Else$Else Ifÿ3ThenäElse$ If 2ThenäElseIf nFunc g()
FunenäElseIf 2Else Else If 1Then¬%lseIf 2Else Else$ If 2ThenäElseIf nFunc g()uncIf nfŞunc2Else ElnäElseäElse==If 3ThenäElse Else