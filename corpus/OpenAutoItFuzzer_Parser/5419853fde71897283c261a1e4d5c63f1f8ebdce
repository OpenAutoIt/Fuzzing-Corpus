[0l If 1ThenäElsEIf 2Else Else Else Iälr=If3ThenäElse Else yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyIf 1ThenäEl2Else ³lse$ If 2ThenäElseIf nFunc g()
FuäElseIf nFunc g()
FuncäElseIf näEl 2Else Else$ If 2ThenäElseIf nFunc g()
FuäElseIf nFunc g()
FuncäElseIf näElsIf 2Else ElsElseIf 2Else Else$ If 2ThenäElseIf nFunc g()
FuncäElseIf nfunc2Else Else Ifÿ3ThenäElseIf 2ThenäEfŠ2Else Else$ If 2ThenäElseIf nFunc g()
Then@minäE If 2ThenäElseIf nFunc g()
F²ncäElseIf nfunc2Else Else Ifÿ3ThenäElseIf 2ThenäElseäElse==If 3ThenäElElse Else